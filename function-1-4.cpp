#include <iostream> 

int sum_two_arrays (int array[], int secondarray[], int n) {
    int arrays_sum = 0;
    if ( n < 1 ){

        else if ( n >= 1) {

            for (i=0; i<n ; i++){
                array_sum += array[i] + secondarray[i];
            }
        }
    }

    return arrays_sum
}