#include <iostream> 

int max_element (int array[], int n){
    int array_maximum = 0;

    if (n < 1) {
        //retain array_maximum = 0 if n < 1 

        else if(n >= 1){
         array_maximum = array[0];
            
            for(i=1; i<n; i++){
                if (array_maximum < array[i]){
                    int array_maximum = array[i]
                }

            }

        }
    }
return array_maximum
}