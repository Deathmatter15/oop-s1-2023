#include <iostream> 

int min_element (int array[], int n){
    int array_minimum = 0;

    if (n < 1) {
        //retain array_minimum = 0 if n < 1 

        else if(n >= 1){
            array_minimum = array[0];
            
            for(i=1; i<n; i++){
                if(array_minimum > array[i]){
                    int array_minimum = array[i]
                }

            }

        }
    }
return array_minimum
}